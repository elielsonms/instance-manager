apiVersion: v1
kind: Service
metadata:
  name: ${INSTANCE}
  namespace: ${INSTANCE}
  labels:
    app: ${INSTANCE}
    managed-by: instance-manager
spec:
  selector:
    app: ${INSTANCE}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
